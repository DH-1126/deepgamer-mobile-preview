# 登录优化 · 新版 UI 原型验收

日期：2026-09-15

## 范围与设计基线

- 实现目录：`代码/前端`，入口 `http://m.deepgamer.localhost:5174/login/password`。
- 按本轮已说明的落点，在本地新版原型实现；保留现有黄黑设计系统，不修改同步线上的 `代码/deep-platform-client` 紫色客户端。
- 截图作为弹窗结构、字段和文案依据；颜色、标题、52px 控件、圆角和居中位置沿用新版原型组件规范。不是紫色整页的像素级复刻。
- 只做本地模拟认证，无真实短信、生产账号接口或线上发布。

## 视觉证据

参考图（均 853 × 1844 px）：

- 注册：`/Users/ttcc/.codex/generated_images/01a01e4d-bf1d-7c80-bd8e-71986d2317d7/exec-d1e67584-c3f9-4838-a177-f1c1ea9da31f.png`
- 重置：`/Users/ttcc/.codex/generated_images/01a01e4d-bf1d-7c80-bd8e-71986d2317d7/exec-32cf4210-f710-4f0f-a21a-dee75a846b34.png`
- 字段细节：`/var/folders/1f/hd22yhc93sb5ft2xsfmzgpz40000gn/T/codex-clipboard-ab742b8f-7e11-4bbc-af62-c6c795d18398.png`

实施截图位于 `/Users/ttcc/工程文件/深度玩家/素材/验收/登录优化-20260915/`：

- `register-verified.png`：未注册引导，未发送验证码。
- `reset-verified.png`：第三次输错后的找回密码，三个字段为空。
- `register-comparison.png` / `reset-comparison.png`：同一图片中左参考、右实施，弹窗区域统一为 361px 宽，已实际打开对照。
- `reset-small-error.png`：320px 宽小屏，确认密码不一致与验证码冷却。

视口：桌面 1280 × 720；移动端 CSS 393 × 852，DPR 1；小屏 CSS 320 × 568。普通截图工具实际截得 393 × 780 / 320 × 496 可见区域，393px 版本完整覆盖弹窗。浏览器 fullPage 截图出现缩放拼接异常，未作为视觉结论依据。

密度归一化：注册参考截取 698 × 673，重置参考截取 696 × 893，等比例缩至 361px 宽；实施直接截取截图中 361px 宽弹窗，不拉伸。对比画布 742 × 590。全屏对照用于检查遮罩、弹窗和原登录页关系；局部合成对照用于检查字段、文字、图标、按钮。

## 五项视觉检查

| 项目 | 结果 |
| --- | --- |
| 字体与层级 | 复用平台系统字体；标题19px，正文14px，字段标签13px，手机号24px，主按钮16px。字体层级清晰，无标题或按钮文字截断。 |
| 布局与间距 | 361px 弹窗，左右至少16px外边距；三字段按新密码、确认新密码、验证码顺序；主按钮52px。相比紫色参考整体更高，属于复用新版52px控件后的明确适配。小屏限定高度、内部可滚动。 |
| 色彩与状态 | 复用黄/浅黄/中性色 token；红色仅用于校验错误；验证码冷却禁用；保留键盘焦点，触摸点击无额外常驻选中状态。 |
| 图标与图像 | 复用原型品牌资产和现有 Lucide 锁、眼睛、关闭图标；未新增假背景或重画品牌。两个密码框独立显隐。 |
| 文案 | 注册准确说明会设置原来已输入的密码；找回直接设置并验证，不再提示跳去另一页面；两个弹窗均无“更换手机号”。测试手机号按表单动态脱敏，不固定展示参考图号码。 |

未发现待修复的 P0/P1/P2 视觉问题。注册截图较早一版包含冷却状态，后补拍未发送状态后再对比；fullPage 截图异常已排除，使用正常截图中的完整弹窗作为证据。

## 交互验收

已通过内置浏览器实际操作：

- 启动/协议/加载后进入密码登录，不绕过既有启动流程。
- 未注册手机号输入密码，弹出注册引导；关闭保留原输入。
- 未获取短信不能确认，错误验证码不能注册；发送后60秒倒计时，关闭重开仍保留冷却。
- 正确验证后自动注册登录；退出后，使用刚才输入的原密码可再次登录。
- 已注册账号前两次输错提示重试，第三次打开重置框。
- 新密码规则与密码设置共用：8–18位且包含字母和数字；两次输入必须相同；短信6位。
- 两个密码框独立显隐；校验失败定位输入框，错误不消费有效验证码。
- 重置成功自动登录并返回 `/profile`；之后旧密码失败，新密码成功。
- “忘记密码？”直接打开同一重置框；未勾选协议先提示同意，确认后回到正确流程。
- Tab 保持在弹窗中，Esc 关闭并恢复触发按钮焦点；支持从客服入口进入既有平台客服页面（入口复用）。
- 320px 小屏没有横向溢出，弹窗 scrollWidth/clientWidth 均288；确认按钮可通过滚动触达并提交成功。

## 工程检查与边界

- 全量 Vitest：633/633 通过；113 个测试文件。新认证仓库测试14项，新增弹窗结构测试3项。
- TypeScript 构建检查通过；Vite 生产构建通过。
- `git diff --check` 通过。
- 初次启动发现旧依赖缓存缺少 `ListCardShell` 导出，重启本地 Vite 并重建缓存后恢复；之后浏览器无新增 error。未修改其他任务的 ProductCard 文件。
- 测试编辑期间出现过一项会话测试参数误改，恢复 `persistSession:false` 后重新运行全部633项通过；未删断言或跳过测试。
- 仍有已有大包体积提示及部分 SSR useLayoutEffect 警告，不属于此次新增登录功能。
- 演示账号、错误计数、验证码 challenge 仅存在内存，刷新即重置，不保存手机号、密码、验证码到 Storage。
- 后台字典尚未接真实接口：`src/data/authPolicy.ts` 提供原型策略 `passwordErrorLimit: 3`，仓库支持注入覆盖；生产限流、短信下发、账号判定、匿名密码重置须后端实现和校验。
- 未做真实手机软键盘/短信自动填充验证；没有修改真实密码。

final result: passed
